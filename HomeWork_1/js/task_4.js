"use strict";

let mode = "normal"; //корректное название переменной

let my_valu3 = 102; //корректное название переменной

let 3my_value3 = "102"; //не корректное название переменной, нельзя начинать с цифры

let __hello__ = "world"; //корректное название переменной

let $$$ = "money"; //корректное название переменной

let !0_world = true; //не корректное название переменной, нельзя использовать спец. символы